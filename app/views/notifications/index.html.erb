<i class="fas fa-angle-double-left faa-passing-reverse animated" style="margin-bottom: 50px">
</i><%= link_to "マイページへ戻る", current_user %><br>
<% notifications = @notifications.where.not(visitor_id: current_user.id) %>
<% if notifications.exists? %>
  <%= render notifications %>
  <%= paginate notifications %>
<% else %>
  <p>通知はありません</p>
<% end %>
